<?php 
/*
$array = array(
    "a", 
    "b"
);

$remplazo = array(
    0 => "dif"
);

$final = array_replace(
    $array,$remplazo
);
var_dump($final);
*/

/*
$datos = "";
if (isset($_POST['dato'])) {
    $datos = $_POST['dato'];
}
if (!empty($datos))
{
    // llena el arreglo, caracter a caracter, y dimensiona el arreglo, 
    //según la cantidad de caracteres, de la cadena de caracteres
    $datos = str_split($_POST['dato']); 
    echo "<pre>";
    print_r($datos);
    echo "</pre>";
    // muestra el contenido del arreglo con for
    for ($i=0; $i<count($datos);$i++)
    {
        echo($datos[$i]);
        echo "<br>";
    }
} 
else 
{
    echo "No
Tú
20:35
$datos = "";
if (isset($_POST['dato'])) {
    $datos = $_POST['dato'];
}
if (!empty($datos))
{
    // llena el arreglo, caracter a caracter, y dimensiona el arreglo, 
    //según la cantidad de caracteres, de la cadena de caracteres
    $datos = str_split($_POST['dato']); 
    echo "<pre>";
    print_r($datos);
    echo "</pre>";
    // muestra el contenido del arreglo con for
    for ($i=0; $i<count($datos);$i++)
    {
        echo($datos[$i]);
        echo "<br>";
    }
} 
else 
{
    echo "No
*/

$array= array(
    "hola" => "adios"
);

var_dump($array);
?>